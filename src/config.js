/* Datos de contacto. Se usan en el formulario, en los links directos y en el pie. */
export const CONTACTO = {
  mail: 'hola@upward.com',      // ← tu mail
  whatsapp: '5491100000000',    // ← código de país + número, sin + ni espacios
}

/* Links del menú. El href tiene que coincidir con el id de cada <section>. */
export const NAV_LINKS = [
  { href: '#metodo', label: 'Método' },
  { href: '#construimos', label: 'Qué construimos' },
  { href: '#proceso', label: 'Proceso' },
  { href: '#clientes', label: 'Clientes' },
  { href: '#faq', label: 'Preguntas' },
]
